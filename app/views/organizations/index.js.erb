<% if @organization.valid? %>
	$("#organizations").append("<%= escape_javascript(render(@organization)) %>");
<% else %>
	$("div#errors").html("<%= escape_javascript(render :partial => 'errors', :locals => {:organization => @organization }) %>");
<% end %>